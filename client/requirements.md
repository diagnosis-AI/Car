## Packages
framer-motion | Essential for the scanning animations and smooth UI transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
The application is RTL (Right-to-Left) for Arabic.
Integration with OpenAI via the scan endpoint is required.
Icons will use Lucide React.
